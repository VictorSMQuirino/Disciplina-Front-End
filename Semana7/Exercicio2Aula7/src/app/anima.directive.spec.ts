import { AnimaDirective } from './anima.directive';

describe('AnimaDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimaDirective();
    expect(directive).toBeTruthy();
  });
});
