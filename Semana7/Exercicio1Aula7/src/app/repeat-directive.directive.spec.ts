import { RepeatDirectiveDirective } from './repeat-directive.directive';

describe('RepeatDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
