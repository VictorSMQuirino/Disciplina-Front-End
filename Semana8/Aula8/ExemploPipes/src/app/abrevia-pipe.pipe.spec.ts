import { AbreviaPipePipe } from './abrevia-pipe.pipe';

describe('AbreviaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AbreviaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
